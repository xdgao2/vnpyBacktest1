t = loadTable("dfs://vnpy", "dominant_mapping")

//查询合约（如 AG2005）的开始和结束日期
// select min(trade_date) as start_date, max(trade_date) as end_date from t 
// where dominant_contract = "AG2002.SHFE"

//指定日期和品种查询主力合约
select last(dominant_contract) from t where trade_date <= 2019.12.10, product_code == `AG
